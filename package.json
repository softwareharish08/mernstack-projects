{
  "name": "todo_app",
  "version": "1.0.0",
  "description": "todo app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm start --prefix todo_client\" \"nodemon index.js --prefix todo_server\""

  },
  "author": "N Harish",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
